/** Minimum listings the public discovery surface must support (acceptance). */
export const MIN_PUBLIC_LISTINGS = 10;

/** Client-side budget for loading discovery listings (acceptance, ms). */
export const DISCOVERY_LOAD_BUDGET_MS = 2000;

/**
 * Discovery home: enough for popular (12) plus per-region carousels (12 each).
 */
export const DISCOVERY_HOME_SEARCH_LIMIT = 60;

/** Search results initial fetch (API allows up to 200). */
export const SEARCH_RESULTS_PAGE_LIMIT = 200;

/** Home wizard activity preview fetch size. */
export const HOME_WIZARD_SEARCH_LIMIT = 60;
