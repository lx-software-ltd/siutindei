openapi: 3.0.3
info:
  title: Admin CRUD API
  version: 0.1.0
  description: |
    CRUD endpoints for managing activities data.

    ## Validation Rules

    All endpoints enforce the following validation rules to ensure data integrity
    and protect against security vulnerabilities.

    ### String Length Limits
    | Field | Max Length |
    |-------|-----------|
    | name | 200 characters |
    | description | 5,000 characters |
    | email | 320 characters |
    | phone_country_code | 2 characters |
    | phone_number | 20 characters |
    | social handles / URLs | 64 (handles) / 2,048 (URLs) |
    | address | 500 characters |
    | media_urls (each) | 2,048 characters |
    | logo_media_url | 2,048 characters |

    ### Array Limits
    | Field | Max Items |
    |-------|----------|
    | media_urls | 20 |
    | languages | 20 |

    ### Numeric Ranges
    | Field | Range |
    |-------|-------|
    | lat | -90 to 90 |
    | lng | -180 to 180 |
    | age_min | 0 to 119 |
    | age_max | 1 to 120 |
    | amount | >= 0 |
    | sessions_count | > 0 |
    | day_of_week_utc | 0-6 (Sunday-Saturday) |
    | start_minutes_utc | 0-1439 |
    | end_minutes_utc | 0-1439 |

    ### Allowed Values
    | Field | Allowed Values |
    |-------|---------------|
    | currency | Any ISO 4217 3-letter code (default HKD, case-insensitive) |
    | languages | en, zh, ja, ko, fr, de, es, pt, it, ru, ar, hi, th, vi, id, ms, tl, nl, pl, tr, yue (Cantonese, ISO 639-3) |
    | pricing_type | per_class, per_sessions, per_hour, per_day, free |
    | schedule_type | weekly |

    ### Contact Validation
    - phone_country_code must be an ISO 3166-1 alpha-2 country code.
    - phone_number must be valid for the selected phone_country_code.
    - email must be a valid email address.
    - social fields accept either an http/https URL or a handle.

paths:
  /v1/admin/organizations:
    get:
      summary: List organizations
      description: Retrieve a paginated list of organizations.
      parameters:
        - name: limit
          in: query
          required: false
          description: Maximum number of items to return (1-200, default 50)
          schema:
            type: integer
            minimum: 1
            maximum: 200
            default: 50
        - name: cursor
          in: query
          required: false
          description: Pagination cursor from previous response
          schema:
            type: string
      responses:
        "200":
          description: List of organizations
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OrganizationListResponse'
    post:
      summary: Create organization
      description: Create a new organization.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/OrganizationCreate'
      responses:
        "201":
          description: Organization created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Organization'
        "400":
          description: Validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ValidationError'

  /v1/admin/organizations/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      summary: Get organization
      responses:
        "200":
          description: Organization details
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Organization'
        "404":
          description: Organization not found
    put:
      summary: Update organization
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/OrganizationUpdate'
      responses:
        "200":
          description: Organization updated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Organization'
        "400":
          description: Validation error
        "404":
          description: Organization not found
    delete:
      summary: Delete organization
      responses:
        "204":
          description: Organization deleted
        "404":
          description: Organization not found

  /v1/admin/organizations/{id}/media:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    post:
      summary: Create organization media upload URL
      description: Generate a presigned URL for uploading organization media.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/MediaUploadRequest'
      responses:
        "200":
          description: Upload URL generated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MediaUploadResponse'
        "400":
          description: Validation error
    delete:
      summary: Delete organization media
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/MediaDeleteRequest'
      responses:
        "204":
          description: Media deleted

  /v1/admin/imports/presign:
    post:
      summary: Create admin import upload URL
      description: |
        Generate a presigned URL for uploading a JSON import file to S3.
        Requires a Cognito JWT in the `admin` or `importer` group.
      security:
        - AdminGroupAuth: []
        - ImporterGroupAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AdminImportPresignRequest'
      responses:
        "200":
          description: Upload URL generated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AdminImportPresignResponse'
        "400":
          description: Validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ValidationError'

  /v1/admin/imports:
    post:
      summary: Process admin import
      description: |
        Process a JSON import file previously uploaded to S3. The import supports
        partial success and returns per-record warnings and errors.
        Requires a Cognito JWT in the `admin` or `importer` group.
        `GET /v1/admin/imports/export` remains admin-only.
      security:
        - AdminGroupAuth: []
        - ImporterGroupAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AdminImportRequest'
      responses:
        "200":
          description: Import processed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AdminImportResponse'
        "400":
          description: Validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ValidationError'

  /v1/admin/imports/export:
    get:
      summary: Export admin data
      description: |
        Generate a presigned download URL for an export JSON file. The exported
        JSON is compatible with the admin import schema. Schedule entries are
        emitted in UTC.
      parameters:
        - name: org_name
          in: query
          required: false
          description: Optional organization name to export a single organization.
          schema:
            type: string
      responses:
        "200":
          description: Export download URL generated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AdminExportResponse'
        "400":
          description: Validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ValidationError'

  /v1/admin/locations:
    get:
      summary: List locations
      parameters:
        - name: limit
          in: query
          required: false
          schema:
            type: integer
            minimum: 1
            maximum: 200
            default: 50
        - name: cursor
          in: query
          required: false
          schema:
            type: string
      responses:
        "200":
          description: List of locations
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/LocationListResponse'
    post:
      summary: Create location
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/LocationCreate'
      responses:
        "201":
          description: Location created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Location'
        "400":
          description: Validation error

  /v1/admin/locations/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      summary: Get location
      responses:
        "200":
          description: Location details
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Location'
        "404":
          description: Location not found
    put:
      summary: Update location
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/LocationUpdate'
      responses:
        "200":
          description: Location updated
        "400":
          description: Validation error
        "404":
          description: Location not found
    delete:
      summary: Delete location
      responses:
        "204":
          description: Location deleted
        "404":
          description: Location not found

  /v1/admin/activity-categories:
    get:
      summary: List activity categories
      description: Retrieve a paginated list of activity categories.
      parameters:
        - name: limit
          in: query
          required: false
          schema:
            type: integer
            minimum: 1
            maximum: 200
            default: 50
        - name: cursor
          in: query
          required: false
          schema:
            type: string
      responses:
        "200":
          description: List of activity categories
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ActivityCategoryListResponse'
    post:
      summary: Create activity category
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ActivityCategoryCreate'
      responses:
        "201":
          description: Activity category created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ActivityCategory'
        "400":
          description: Validation error

  /v1/admin/activity-categories/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      summary: Get activity category
      responses:
        "200":
          description: Activity category details
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ActivityCategory'
        "404":
          description: Activity category not found
    put:
      summary: Update activity category
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ActivityCategoryUpdate'
      responses:
        "200":
          description: Activity category updated
        "400":
          description: Validation error
        "404":
          description: Activity category not found
    delete:
      summary: Delete activity category
      responses:
        "204":
          description: Activity category deleted
        "400":
          description: Validation error
        "404":
          description: Activity category not found

  /v1/admin/feedback-labels:
    get:
      summary: List feedback labels
      description: Retrieve a paginated list of feedback labels.
      parameters:
        - name: limit
          in: query
          required: false
          schema:
            type: integer
            minimum: 1
            maximum: 200
            default: 50
        - name: cursor
          in: query
          required: false
          schema:
            type: string
      responses:
        "200":
          description: List of feedback labels
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/FeedbackLabelListResponse'
    post:
      summary: Create feedback label
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/FeedbackLabelCreate'
      responses:
        "201":
          description: Feedback label created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/FeedbackLabel'
        "400":
          description: Validation error

  /v1/admin/feedback-labels/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      summary: Get feedback label
      responses:
        "200":
          description: Feedback label details
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/FeedbackLabel'
        "404":
          description: Feedback label not found
    put:
      summary: Update feedback label
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/FeedbackLabelUpdate'
      responses:
        "200":
          description: Feedback label updated
        "400":
          description: Validation error
        "404":
          description: Feedback label not found
    delete:
      summary: Delete feedback label
      responses:
        "204":
          description: Feedback label deleted
        "400":
          description: Validation error
        "404":
          description: Feedback label not found

  /v1/admin/organization-feedback:
    get:
      summary: List organization feedback
      description: Retrieve a paginated list of feedback entries.
      parameters:
        - name: limit
          in: query
          required: false
          schema:
            type: integer
            minimum: 1
            maximum: 200
            default: 50
        - name: cursor
          in: query
          required: false
          schema:
            type: string
      responses:
        "200":
          description: List of feedback entries
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OrganizationFeedbackListResponse'
    post:
      summary: Create organization feedback
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/OrganizationFeedbackCreate'
      responses:
        "201":
          description: Feedback entry created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OrganizationFeedback'
        "400":
          description: Validation error

  /v1/admin/organization-feedback/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      summary: Get organization feedback
      responses:
        "200":
          description: Feedback entry details
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OrganizationFeedback'
        "404":
          description: Feedback entry not found
    put:
      summary: Update organization feedback
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/OrganizationFeedbackUpdate'
      responses:
        "200":
          description: Feedback entry updated
        "400":
          description: Validation error
        "404":
          description: Feedback entry not found
    delete:
      summary: Delete organization feedback
      responses:
        "204":
          description: Feedback entry deleted
        "400":
          description: Validation error
        "404":
          description: Feedback entry not found

  /v1/admin/activities:
    get:
      summary: List activities
      parameters:
        - name: limit
          in: query
          required: false
          schema:
            type: integer
            minimum: 1
            maximum: 200
            default: 50
        - name: cursor
          in: query
          required: false
          schema:
            type: string
      responses:
        "200":
          description: List of activities
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ActivityListResponse'
    post:
      summary: Create activity
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ActivityCreate'
      responses:
        "201":
          description: Activity created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Activity'
        "400":
          description: Validation error

  /v1/admin/activities/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      summary: Get activity
      responses:
        "200":
          description: Activity details
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Activity'
        "404":
          description: Activity not found
    put:
      summary: Update activity
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ActivityUpdate'
      responses:
        "200":
          description: Activity updated
        "400":
          description: Validation error
        "404":
          description: Activity not found
    delete:
      summary: Delete activity
      responses:
        "204":
          description: Activity deleted
        "404":
          description: Activity not found

  /v1/admin/pricing:
    get:
      summary: List pricing entries
      parameters:
        - name: limit
          in: query
          required: false
          schema:
            type: integer
            minimum: 1
            maximum: 200
            default: 50
        - name: cursor
          in: query
          required: false
          schema:
            type: string
      responses:
        "200":
          description: List of pricing entries
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PricingListResponse'
    post:
      summary: Create pricing entry
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PricingCreate'
      responses:
        "201":
          description: Pricing entry created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Pricing'
        "400":
          description: Validation error

  /v1/admin/pricing/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      summary: Get pricing entry
      responses:
        "200":
          description: Pricing details
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Pricing'
        "404":
          description: Pricing not found
    put:
      summary: Update pricing entry
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PricingUpdate'
      responses:
        "200":
          description: Pricing updated
        "400":
          description: Validation error
        "404":
          description: Pricing not found
    delete:
      summary: Delete pricing entry
      responses:
        "204":
          description: Pricing deleted
        "404":
          description: Pricing not found

  /v1/admin/schedules:
    get:
      summary: List schedules
      parameters:
        - name: limit
          in: query
          required: false
          schema:
            type: integer
            minimum: 1
            maximum: 200
            default: 50
        - name: cursor
          in: query
          required: false
          schema:
            type: string
      responses:
        "200":
          description: List of schedules
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ScheduleListResponse'
    post:
      summary: Create schedule
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ScheduleCreate'
      responses:
        "201":
          description: Schedule created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Schedule'
        "400":
          description: Validation error

  /v1/admin/schedules/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      summary: Get schedule
      responses:
        "200":
          description: Schedule details
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Schedule'
        "404":
          description: Schedule not found
    put:
      summary: Update schedule
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ScheduleUpdate'
      responses:
        "200":
          description: Schedule updated
        "400":
          description: Validation error
        "404":
          description: Schedule not found
    delete:
      summary: Delete schedule
      responses:
        "204":
          description: Schedule deleted
        "404":
          description: Schedule not found

  /v1/admin/users/{username}/groups:
    parameters:
      - name: username
        in: path
        required: true
        schema:
          type: string
    post:
      summary: Add user to group
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                group:
                  type: string
                  description: Group name (defaults to 'admin')
      responses:
        "200":
          description: User added to group
    delete:
      summary: Remove user from group
      responses:
        "200":
          description: User removed from group

  # --- Cognito Users Management (Admin Only) ---

  /v1/admin/cognito-users:
    get:
      summary: List Cognito users
      description: |
        List all Cognito user pool users with their group memberships.
        Used for manager selection and user management.
      parameters:
        - name: limit
          in: query
          required: false
          description: Maximum number of users to return (1-60, default 50)
          schema:
            type: integer
            minimum: 1
            maximum: 60
            default: 50
        - name: pagination_token
          in: query
          required: false
          description: Pagination token from previous response
          schema:
            type: string
      responses:
        "200":
          description: List of Cognito users
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CognitoUserListResponse'

  /v1/admin/cognito-users/{username}:
    parameters:
      - name: username
        in: path
        required: true
        schema:
          type: string
    delete:
      summary: Delete Cognito user
      description: |
        Delete a Cognito user and transfer their managed organizations
        to the requesting admin user. The admin cannot delete themselves.
      responses:
        "200":
          description: User deleted and organizations transferred
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    description: Deletion status (e.g., "deleted")
                  username:
                    type: string
                    description: Username of the deleted user
                  user_sub:
                    type: string
                    description: Cognito sub of the deleted user
                  transferred_organizations_count:
                    type: integer
                    description: Number of organizations transferred to the admin
                  fallback_manager_id:
                    type: string
                    description: Cognito sub of the admin who received the transferred organizations
        "400":
          description: Cannot delete yourself
        "404":
          description: Cognito user not found

  # --- Tickets Management (Admin Only) ---

  /v1/admin/tickets:
    get:
      summary: List tickets
      description: |
        List all tickets for admin review. Supports filtering by
        ticket_type, status, and cursor pagination.
      parameters:
        - name: ticket_type
          in: query
          required: false
          description: Filter by ticket type
          schema:
            type: string
            enum: [access_request, organization_suggestion, organization_feedback]
        - name: status
          in: query
          required: false
          description: Filter by ticket status
          schema:
            type: string
            enum: [pending, approved, rejected]
        - name: limit
          in: query
          required: false
          schema:
            type: integer
            minimum: 1
            maximum: 200
            default: 50
        - name: cursor
          in: query
          required: false
          schema:
            type: string
      responses:
        "200":
          description: List of tickets
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TicketListResponse'

  /v1/admin/tickets/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    put:
      summary: Review ticket
      description: |
        Approve or reject a ticket. Approval behaviour depends on the
        ticket_type. The request body may include organization_id,
        create_organization, and admin_notes.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/TicketReview'
      responses:
        "200":
          description: Ticket reviewed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TicketReviewResponse'
        "400":
          description: Validation error
        "404":
          description: Ticket not found

  # --- Audit Logs (Admin Only, Read-Only) ---

  /v1/admin/audit-logs:
    get:
      summary: List audit logs
      description: |
        Query audit logs with optional filtering by table, record, user,
        action type, and timestamp. Returns paginated results.
      parameters:
        - name: table
          in: query
          required: false
          description: Filter by table name (required if record_id is provided)
          schema:
            type: string
        - name: record_id
          in: query
          required: false
          description: Filter by specific record ID (requires table parameter)
          schema:
            type: string
        - name: user_id
          in: query
          required: false
          description: Filter by user who made the change
          schema:
            type: string
        - name: action
          in: query
          required: false
          description: Filter by action type
          schema:
            type: string
            enum: [INSERT, UPDATE, DELETE]
        - name: since
          in: query
          required: false
          description: Filter by timestamp (ISO 8601 format)
          schema:
            type: string
            format: date-time
        - name: limit
          in: query
          required: false
          schema:
            type: integer
            minimum: 1
            maximum: 200
            default: 50
        - name: cursor
          in: query
          required: false
          schema:
            type: string
      responses:
        "200":
          description: List of audit log entries
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AuditLogListResponse'

  /v1/admin/audit-logs/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      summary: Get audit log entry
      responses:
        "200":
          description: Audit log entry details
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AuditLogEntry'
        "404":
          description: Audit log entry not found

  # --- Partner API Key Management (Admin Group) ---

  /v1/admin/api-keys:
    get:
      summary: List partner API keys
      description: |
        Paginated list of partner API keys used to authenticate the
        `/v1/partner/*` endpoints (see `partner.yaml`). Only metadata is
        returned; plaintext key values are never stored.
      parameters:
        - name: limit
          in: query
          required: false
          schema:
            type: integer
            minimum: 1
            maximum: 200
            default: 50
        - name: cursor
          in: query
          required: false
          schema:
            type: string
      responses:
        "200":
          description: List of partner API keys
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiKeyListResponse'
        "403":
          description: Not an admin
    post:
      summary: Create partner API key
      description: |
        Generate a new partner API key. The plaintext key value is
        returned only in this response and cannot be retrieved again.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ApiKeyCreate'
      responses:
        "201":
          description: Created API key including the one-time plaintext value
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiKeyCreated'
        "400":
          description: Validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ValidationError'
        "403":
          description: Not an admin

  /v1/admin/api-keys/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      summary: Get partner API key metadata
      responses:
        "200":
          description: API key metadata
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiKey'
        "404":
          description: API key not found
    delete:
      summary: Revoke partner API key
      description: |
        Revoke a key (idempotent soft delete; the record is kept for the
        audit trail). Revoked keys stop working within the authorizer
        cache window (up to 5 minutes).
      responses:
        "200":
          description: The revoked API key metadata
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiKey'
        "404":
          description: API key not found

  # --- Manager Routes (Admin or Manager Group) ---

  /v1/manager/organizations:
    get:
      summary: List managed organizations
      description: |
        List organizations managed by the authenticated user.
        Results are filtered by the user's organization management.
      parameters:
        - name: limit
          in: query
          required: false
          schema:
            type: integer
            minimum: 1
            maximum: 200
            default: 50
        - name: cursor
          in: query
          required: false
          schema:
            type: string
      responses:
        "200":
          description: List of managed organizations
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OrganizationListResponse'
    post:
      summary: Create organization (manager)
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/OrganizationCreate'
      responses:
        "201":
          description: Organization created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Organization'
        "400":
          description: Validation error

  /v1/manager/organizations/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      summary: Get managed organization
      responses:
        "200":
          description: Organization details
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Organization'
        "403":
          description: Not a manager of this organization
        "404":
          description: Organization not found
    put:
      summary: Update managed organization
      description: Managers cannot change the manager_id field.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/OrganizationUpdate'
      responses:
        "200":
          description: Organization updated
        "400":
          description: Validation error
        "403":
          description: Not a manager of this organization
        "404":
          description: Organization not found
    delete:
      summary: Delete managed organization
      responses:
        "204":
          description: Organization deleted
        "403":
          description: Not a manager of this organization
        "404":
          description: Organization not found

  /v1/manager/locations:
    get:
      summary: List locations in managed organizations
      parameters:
        - name: limit
          in: query
          required: false
          schema:
            type: integer
            minimum: 1
            maximum: 200
            default: 50
        - name: cursor
          in: query
          required: false
          schema:
            type: string
      responses:
        "200":
          description: List of locations
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/LocationListResponse'
    post:
      summary: Create location in managed organization
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/LocationCreate'
      responses:
        "201":
          description: Location created
        "400":
          description: Validation error
        "403":
          description: Not a manager of the target organization

  /v1/manager/locations/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      summary: Get location in managed organization
      responses:
        "200":
          description: Location details
        "403":
          description: Not a manager of this organization
        "404":
          description: Location not found
    put:
      summary: Update location in managed organization
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/LocationUpdate'
      responses:
        "200":
          description: Location updated
        "400":
          description: Validation error
        "403":
          description: Not a manager of this organization
        "404":
          description: Location not found
    delete:
      summary: Delete location in managed organization
      responses:
        "204":
          description: Location deleted
        "403":
          description: Not a manager of this organization
        "404":
          description: Location not found

  /v1/manager/activities:
    get:
      summary: List activities in managed organizations
      parameters:
        - name: limit
          in: query
          required: false
          schema:
            type: integer
            minimum: 1
            maximum: 200
            default: 50
        - name: cursor
          in: query
          required: false
          schema:
            type: string
      responses:
        "200":
          description: List of activities
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ActivityListResponse'
    post:
      summary: Create activity in managed organization
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ActivityCreate'
      responses:
        "201":
          description: Activity created
        "400":
          description: Validation error
        "403":
          description: Not a manager of the target organization

  /v1/manager/activities/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      summary: Get activity in managed organization
      responses:
        "200":
          description: Activity details
        "403":
          description: Not a manager of this organization
        "404":
          description: Activity not found
    put:
      summary: Update activity in managed organization
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ActivityUpdate'
      responses:
        "200":
          description: Activity updated
        "400":
          description: Validation error
        "403":
          description: Not a manager of this organization
        "404":
          description: Activity not found
    delete:
      summary: Delete activity in managed organization
      responses:
        "204":
          description: Activity deleted
        "403":
          description: Not a manager of this organization
        "404":
          description: Activity not found

  /v1/manager/pricing:
    get:
      summary: List pricing in managed organizations
      parameters:
        - name: limit
          in: query
          required: false
          schema:
            type: integer
            minimum: 1
            maximum: 200
            default: 50
        - name: cursor
          in: query
          required: false
          schema:
            type: string
      responses:
        "200":
          description: List of pricing entries
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PricingListResponse'
    post:
      summary: Create pricing in managed organization
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PricingCreate'
      responses:
        "201":
          description: Pricing created
        "400":
          description: Validation error
        "403":
          description: Not a manager of the target organization

  /v1/manager/pricing/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      summary: Get pricing in managed organization
      responses:
        "200":
          description: Pricing details
        "403":
          description: Not a manager of this organization
        "404":
          description: Pricing not found
    put:
      summary: Update pricing in managed organization
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PricingUpdate'
      responses:
        "200":
          description: Pricing updated
        "400":
          description: Validation error
        "403":
          description: Not a manager of this organization
        "404":
          description: Pricing not found
    delete:
      summary: Delete pricing in managed organization
      responses:
        "204":
          description: Pricing deleted
        "403":
          description: Not a manager of this organization
        "404":
          description: Pricing not found

  /v1/manager/schedules:
    get:
      summary: List schedules in managed organizations
      parameters:
        - name: limit
          in: query
          required: false
          schema:
            type: integer
            minimum: 1
            maximum: 200
            default: 50
        - name: cursor
          in: query
          required: false
          schema:
            type: string
      responses:
        "200":
          description: List of schedules
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ScheduleListResponse'
    post:
      summary: Create schedule in managed organization
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ScheduleCreate'
      responses:
        "201":
          description: Schedule created
        "400":
          description: Validation error
        "403":
          description: Not a manager of the target organization

  /v1/manager/schedules/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      summary: Get schedule in managed organization
      responses:
        "200":
          description: Schedule details
        "403":
          description: Not a manager of this organization
        "404":
          description: Schedule not found
    put:
      summary: Update schedule in managed organization
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ScheduleUpdate'
      responses:
        "200":
          description: Schedule updated
        "400":
          description: Validation error
        "403":
          description: Not a manager of this organization
        "404":
          description: Schedule not found
    delete:
      summary: Delete schedule in managed organization
      responses:
        "204":
          description: Schedule deleted
        "403":
          description: Not a manager of this organization
        "404":
          description: Schedule not found

  # --- Geographic Areas Management (Admin Only) ---

  /v1/admin/areas:
    get:
      summary: List all geographic areas
      description: |
        Returns the full geographic area tree including inactive countries.
        Admin-only endpoint for managing area activation.
      responses:
        "200":
          description: Full area tree
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AreaTreeResponse'

  /v1/admin/areas/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    patch:
      summary: Toggle area active status
      description: |
        Activate or deactivate a geographic area (typically a country).
        When a country is deactivated, it and its children are hidden
        from user-facing endpoints.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [active]
              properties:
                active:
                  type: boolean
                  description: New active state
      responses:
        "200":
          description: Area updated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GeographicArea'
        "404":
          description: Area not found

  # --- User Routes (Any Authenticated User) ---

  /v1/user/address-search:
    get:
      summary: Address autocomplete
      description: |
        Proxy to Nominatim for address autocomplete suggestions.
      security:
        - UserAuth: []
      parameters:
        - name: q
          in: query
          required: true
          description: Search query (min 3 characters)
          schema:
            type: string
            minLength: 3
            maxLength: 500
        - name: countrycodes
          in: query
          required: false
          description: Comma-separated ISO 3166-1 alpha-2 codes
          schema:
            type: string
        - name: limit
          in: query
          required: false
          description: Maximum suggestions (1-10, default 5)
          schema:
            type: integer
            minimum: 1
            maximum: 10
            default: 5
      responses:
        "200":
          description: Address suggestions
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AddressSearchResponse'
        "400":
          description: Validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ValidationError'
        "502":
          description: Upstream address provider error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ValidationError'

  /v1/user/areas:
    get:
      summary: List active geographic areas
      description: |
        Returns the geographic area tree for active countries only.
        Used by frontend for cascading country/district dropdowns.
      responses:
        "200":
          description: Active area tree
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AreaTreeResponse'

  /v1/user/activity-categories:
    get:
      summary: List activity categories
      description: |
        Returns the activity category tree for use in selection UIs.
        Available to any authenticated user.
      responses:
        "200":
          description: Activity category tree
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ActivityCategoryTreeResponse'

  /v1/user/access-request:
    get:
      summary: Check access request status
      description: |
        Check if the authenticated user has a pending manager access request
        and get their managed organizations count.
      responses:
        "200":
          description: User's access request status
          content:
            application/json:
              schema:
                type: object
                properties:
                  has_pending_request:
                    type: boolean
                  pending_request:
                    $ref: '#/components/schemas/Ticket'
                    nullable: true
                  organizations_count:
                    type: integer
                    description: Number of organizations the user manages
    post:
      summary: Submit access request
      description: |
        Submit a request to become a manager of an organization.
        Only one pending request per user is allowed.
        Request is published to SNS for async processing.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AccessRequestCreate'
      responses:
        "202":
          description: Request submitted for processing
          content:
            application/json:
              schema:
                type: object
                properties:
                  message:
                    type: string
                  ticket_id:
                    type: string
                    description: Unique ticket ID (e.g., R00001)
        "409":
          description: User already has a pending access request

  /v1/user/organization-suggestion:
    get:
      summary: Get suggestion history
      description: |
        Get the authenticated user's organization suggestion history
        and check if they have a pending suggestion.
      responses:
        "200":
          description: User's suggestion history
          content:
            application/json:
              schema:
                type: object
                properties:
                  has_pending_suggestion:
                    type: boolean
                  suggestions:
                    type: array
                    items:
                      $ref: '#/components/schemas/Ticket'
    post:
      summary: Submit organization suggestion
      description: |
        Suggest a new organization/place for the platform.
        Any authenticated user can submit suggestions.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/OrganizationSuggestionCreate'
      responses:
        "202":
          description: Suggestion submitted
          content:
            application/json:
              schema:
                type: object
                properties:
                  message:
                    type: string
                  ticket_id:
                    type: string
                    description: Unique ticket ID (e.g., S00001)
        "409":
          description: User already has a pending suggestion

  /v1/user/feedback-labels:
    get:
      summary: List feedback labels
      description: Retrieve feedback labels for user selection.
      responses:
        "200":
          description: List of feedback labels
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/FeedbackLabelListResponse'

  /v1/user/organization-feedback:
    get:
      summary: Get feedback history
      description: |
        Get the authenticated user's feedback history and check
        for pending submissions.
      responses:
        "200":
          description: User feedback history
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UserFeedbackResponse'
    post:
      summary: Submit organization feedback
      description: |
        Submit feedback for an organization. Only one pending
        feedback submission is allowed at a time.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UserFeedbackCreate'
      responses:
        "202":
          description: Feedback submitted
          content:
            application/json:
              schema:
                type: object
                properties:
                  message:
                    type: string
                  ticket_id:
                    type: string
        "409":
          description: User already has a pending feedback submission

  /v1/user/organizations:
    get:
      summary: Search organizations
      description: Search organizations by name for feedback selection.
      parameters:
        - name: q
          in: query
          required: false
          description: Search query string
          schema:
            type: string
        - name: limit
          in: query
          required: false
          schema:
            type: integer
            minimum: 1
            maximum: 50
            default: 20
      responses:
        "200":
          description: Organization matches
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OrganizationLookupListResponse'

  # --- Health Check ---

  /health:
    get:
      summary: Health check
      description: |
        Service health check endpoint. Returns overall health status,
        including database connectivity and configuration checks.
        Requires IAM authentication.
      security:
        - IAMAuth: []
      parameters:
        - name: details
          in: query
          required: false
          description: Set to 'true' to include detailed check information
          schema:
            type: string
            enum: ["true", "false"]
      responses:
        "200":
          description: Service is healthy
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HealthStatus'
        "503":
          description: Service is unhealthy
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HealthStatus'

components:
  securitySchemes:
    AdminGroupAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT
      description: Cognito JWT token, user must be in 'admin' group
    ImporterGroupAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT
      description: |
        Cognito JWT token, user must be in the 'importer' or 'admin' group.
        Allowed only on POST /v1/admin/imports and POST /v1/admin/imports/presign.
        The importer app client enables ALLOW_ADMIN_USER_PASSWORD_AUTH for
        AdminInitiateAuth (board catalog import).
    ManagerGroupAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT
      description: Cognito JWT token, user must be in 'admin' or 'manager' group
    UserAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT
      description: Cognito JWT token, any authenticated user
    IAMAuth:
      type: apiKey
      in: header
      name: Authorization
      description: AWS IAM Signature V4
  schemas:
    ValidationError:
      type: object
      required: [error]
      properties:
        error:
          type: string
          description: Error message
        detail:
          type: string
          description: Additional details (e.g., field name)

    TranslationMap:
      type: object
      additionalProperties:
        type: string
      description: |
        Language map for translated text. The server always includes English as "en".
        Additional entries must use ISO 639-1 codes plus "yue" (Cantonese, ISO 639-3).

    AdminImportFile:
      type: object
      required: [organizations]
      properties:
        organizations:
          type: array
          items:
            $ref: '#/components/schemas/AdminImportOrganization'

    AdminImportOrganization:
      type: object
      required: [name]
      properties:
        name:
          type: string
          maxLength: 200
          description: Organization name (unique).
        description:
          type: string
          maxLength: 5000
        name_translations:
          $ref: '#/components/schemas/TranslationMap'
        description_translations:
          $ref: '#/components/schemas/TranslationMap'
        manager_id:
          type: string
          description: Cognito user sub (required for new orgs).
        phone_country_code:
          type: string
          maxLength: 2
        phone_number:
          type: string
          maxLength: 20
        email:
          type: string
          maxLength: 320
        whatsapp:
          type: string
          maxLength: 2048
        facebook:
          type: string
          maxLength: 2048
        instagram:
          type: string
          maxLength: 2048
        tiktok:
          type: string
          maxLength: 2048
        twitter:
          type: string
          maxLength: 2048
        xiaohongshu:
          type: string
          maxLength: 2048
        wechat:
          type: string
          maxLength: 2048
        media_urls:
          type: array
          items:
            type: string
        logo_media_url:
          type: string
        locations:
          type: array
          items:
            $ref: '#/components/schemas/AdminImportLocation'
        activities:
          type: array
          items:
            $ref: '#/components/schemas/AdminImportActivity'

    AdminImportLocation:
      type: object
      required: [name, area_id]
      properties:
        name:
          type: string
          maxLength: 500
          description: |
            Location name used for upsert. Stored as address in the database and
            must be unique within the organization.
        address:
          type: string
          maxLength: 500
          description: Optional address (must match name if provided).
        area_id:
          type: string
          format: uuid
        lat:
          type: number
          minimum: -90
          maximum: 90
        lng:
          type: number
          minimum: -180
          maximum: 180

    AdminImportActivity:
      type: object
      required: [name, category_id, age_min, age_max]
      properties:
        name:
          type: string
          maxLength: 200
        description:
          type: string
          maxLength: 5000
        name_translations:
          $ref: '#/components/schemas/TranslationMap'
        description_translations:
          $ref: '#/components/schemas/TranslationMap'
        category_id:
          type: string
          format: uuid
        age_min:
          type: integer
          minimum: 0
          maximum: 119
        age_max:
          type: integer
          minimum: 1
          maximum: 120
        pricing:
          type: array
          items:
            $ref: '#/components/schemas/AdminImportPricing'
        schedules:
          type: array
          items:
            $ref: '#/components/schemas/AdminImportSchedule'

    AdminImportPricing:
      type: object
      required: [location_name, pricing_type]
      properties:
        location_name:
          type: string
          description: Location name to link pricing with.
        pricing_type:
          type: string
          enum: [per_class, per_sessions, per_hour, per_day, free]
        amount:
          type: number
          minimum: 0
        currency:
          type: string
        sessions_count:
          type: integer
          minimum: 1
        free_trial_class_offered:
          type: boolean

    AdminImportSchedule:
      type: object
      required: [location_name, timezone, weekly_entries]
      properties:
        location_name:
          type: string
        timezone:
          type: string
          description: IANA timezone identifier (e.g., Asia/Hong_Kong).
        languages:
          type: array
          items:
            type: string
        weekly_entries:
          type: array
          items:
            $ref: '#/components/schemas/AdminImportScheduleEntry'

    AdminImportScheduleEntry:
      type: object
      required: [day_of_week, start_time, end_time]
      properties:
        day_of_week:
          type: integer
          minimum: 0
          maximum: 6
          description: Local day of week (0=Sunday).
        start_time:
          type: string
          description: Local start time in HH:MM.
        end_time:
          type: string
          description: Local end time in HH:MM.

    AdminImportPresignRequest:
      type: object
      required: [file_name, content_type]
      properties:
        file_name:
          type: string
          description: Filename ending in .json.
        content_type:
          type: string
          description: Must be application/json.

    AdminImportPresignResponse:
      type: object
      required: [upload_url, object_key, expires_in]
      properties:
        upload_url:
          type: string
        object_key:
          type: string
        expires_in:
          type: integer

    AdminImportRequest:
      type: object
      required: [object_key]
      properties:
        object_key:
          type: string
          description: S3 object key returned from the presign endpoint.

    AdminImportCounts:
      type: object
      required: [created, updated, failed, skipped]
      properties:
        created:
          type: integer
        updated:
          type: integer
        failed:
          type: integer
        skipped:
          type: integer

    AdminImportSummary:
      type: object
      required:
        - organizations
        - locations
        - activities
        - pricing
        - schedules
        - warnings
        - errors
      properties:
        organizations:
          $ref: '#/components/schemas/AdminImportCounts'
        locations:
          $ref: '#/components/schemas/AdminImportCounts'
        activities:
          $ref: '#/components/schemas/AdminImportCounts'
        pricing:
          $ref: '#/components/schemas/AdminImportCounts'
        schedules:
          $ref: '#/components/schemas/AdminImportCounts'
        warnings:
          type: integer
        errors:
          type: integer

    AdminImportError:
      type: object
      required: [message]
      properties:
        message:
          type: string
        field:
          type: string
          nullable: true

    AdminImportResult:
      type: object
      required: [type, key, status, warnings, errors]
      properties:
        type:
          type: string
          enum: [organizations, locations, activities, pricing, schedules]
        key:
          type: string
        status:
          type: string
          enum: [created, updated, failed, skipped]
        id:
          type: string
          nullable: true
        path:
          type: string
          nullable: true
        warnings:
          type: array
          items:
            type: string
        errors:
          type: array
          items:
            $ref: '#/components/schemas/AdminImportError'

    AdminImportResponse:
      type: object
      required: [summary, results, file_warnings]
      properties:
        summary:
          $ref: '#/components/schemas/AdminImportSummary'
        results:
          type: array
          items:
            $ref: '#/components/schemas/AdminImportResult'
        file_warnings:
          type: array
          items:
            type: string

    AdminExportResponse:
      type: object
      required: [download_url, object_key, file_name, expires_in]
      properties:
        download_url:
          type: string
        object_key:
          type: string
        file_name:
          type: string
        expires_in:
          type: integer
        warnings:
          type: array
          items:
            type: string

    # Organization schemas
    OrganizationCreate:
      type: object
      required:
        - name
        - manager_id
      properties:
        name:
          type: string
          maxLength: 200
          description: Organization name (required, max 200 characters)
        description:
          type: string
          maxLength: 5000
          description: Organization description (optional, max 5,000 characters)
        name_translations:
          $ref: '#/components/schemas/TranslationMap'
          description: Non-English name translations (language map)
        description_translations:
          $ref: '#/components/schemas/TranslationMap'
          description: Non-English description translations (language map)
        manager_id:
          type: string
          description: Cognito user sub of the organization manager (required)
        phone_country_code:
          type: string
          maxLength: 2
          description: ISO 3166-1 alpha-2 country code for phone number
        phone_number:
          type: string
          maxLength: 20
          description: National phone number digits (no country code)
        email:
          type: string
          maxLength: 320
          description: Contact email address
        whatsapp:
          type: string
          maxLength: 2048
          description: WhatsApp handle or URL
        facebook:
          type: string
          maxLength: 2048
          description: Facebook handle or URL
        instagram:
          type: string
          maxLength: 2048
          description: Instagram handle or URL
        tiktok:
          type: string
          maxLength: 2048
          description: TikTok handle or URL
        twitter:
          type: string
          maxLength: 2048
          description: X handle or URL
        xiaohongshu:
          type: string
          maxLength: 2048
          description: Xiaohongshu handle or URL
        wechat:
          type: string
          maxLength: 2048
          description: WeChat handle or URL
        media_urls:
          type: array
          maxItems: 20
          items:
            type: string
            format: uri
            maxLength: 2048
          description: |
            List of media URLs (optional, max 20 URLs).
            Each URL must use http or https scheme and be max 2,048 characters.
        logo_media_url:
          type: string
          format: uri
          maxLength: 2048
          description: |
            Optional logo media URL. Must match one of the media_urls values.

    OrganizationUpdate:
      type: object
      properties:
        name:
          type: string
          maxLength: 200
          description: Organization name (max 200 characters)
        description:
          type: string
          maxLength: 5000
          description: Organization description (max 5,000 characters)
        name_translations:
          $ref: '#/components/schemas/TranslationMap'
          description: Non-English name translations (language map)
        description_translations:
          $ref: '#/components/schemas/TranslationMap'
          description: Non-English description translations (language map)
        manager_id:
          type: string
          description: Cognito user sub of the organization manager
        phone_country_code:
          type: string
          maxLength: 2
          description: ISO 3166-1 alpha-2 country code for phone number
        phone_number:
          type: string
          maxLength: 20
          description: National phone number digits (no country code)
        email:
          type: string
          maxLength: 320
          description: Contact email address
        whatsapp:
          type: string
          maxLength: 2048
          description: WhatsApp handle or URL
        facebook:
          type: string
          maxLength: 2048
          description: Facebook handle or URL
        instagram:
          type: string
          maxLength: 2048
          description: Instagram handle or URL
        tiktok:
          type: string
          maxLength: 2048
          description: TikTok handle or URL
        twitter:
          type: string
          maxLength: 2048
          description: X handle or URL
        xiaohongshu:
          type: string
          maxLength: 2048
          description: Xiaohongshu handle or URL
        wechat:
          type: string
          maxLength: 2048
          description: WeChat handle or URL
        media_urls:
          type: array
          maxItems: 20
          items:
            type: string
            format: uri
            maxLength: 2048
        logo_media_url:
          type: string
          format: uri
          maxLength: 2048
          description: |
            Optional logo media URL. Must match one of the media_urls values.

    Organization:
      type: object
      required:
        - id
        - name
        - name_translations
        - description_translations
        - manager_id
        - media_urls
        - created_at
        - updated_at
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string
        description:
          type: string
          nullable: true
        name_translations:
          $ref: '#/components/schemas/TranslationMap'
        description_translations:
          $ref: '#/components/schemas/TranslationMap'
        manager_id:
          type: string
          description: Cognito user sub of the organization manager
        phone_country_code:
          type: string
          nullable: true
          description: ISO 3166-1 alpha-2 country code for phone number
        phone_number:
          type: string
          nullable: true
          description: National phone number digits (no country code)
        email:
          type: string
          nullable: true
          description: Contact email address
        whatsapp:
          type: string
          nullable: true
          description: WhatsApp handle or URL
        facebook:
          type: string
          nullable: true
          description: Facebook handle or URL
        instagram:
          type: string
          nullable: true
          description: Instagram handle or URL
        tiktok:
          type: string
          nullable: true
          description: TikTok handle or URL
        twitter:
          type: string
          nullable: true
          description: X handle or URL
        xiaohongshu:
          type: string
          nullable: true
          description: Xiaohongshu handle or URL
        wechat:
          type: string
          nullable: true
          description: WeChat handle or URL
        media_urls:
          type: array
          items:
            type: string
        logo_media_url:
          type: string
          format: uri
          nullable: true
          description: Optional logo media URL.
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time

    OrganizationListResponse:
      type: object
      required: [items]
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/Organization'
        next_cursor:
          type: string
          nullable: true

    # Location schemas
    LocationCreate:
      type: object
      required:
        - org_id
        - area_id
      properties:
        org_id:
          type: string
          format: uuid
          description: Organization UUID (required)
        area_id:
          type: string
          format: uuid
          description: Geographic area UUID (required, must be a leaf node)
        address:
          type: string
          maxLength: 500
          description: Full address (optional, max 500 characters)
        lat:
          type: number
          minimum: -90
          maximum: 90
          description: Latitude (-90 to 90)
        lng:
          type: number
          minimum: -180
          maximum: 180
          description: Longitude (-180 to 180)

    LocationUpdate:
      type: object
      properties:
        area_id:
          type: string
          format: uuid
          description: Geographic area UUID (must be a leaf node)
        address:
          type: string
          maxLength: 500
          description: Full address (max 500 characters)
        lat:
          type: number
          minimum: -90
          maximum: 90
          description: Latitude (-90 to 90)
        lng:
          type: number
          minimum: -180
          maximum: 180
          description: Longitude (-180 to 180)

    Location:
      type: object
      required: [id, org_id, area_id, created_at, updated_at]
      properties:
        id:
          type: string
          format: uuid
        org_id:
          type: string
          format: uuid
        area_id:
          type: string
          format: uuid
          description: Geographic area UUID (leaf node)
        address:
          type: string
          nullable: true
        lat:
          type: number
          nullable: true
        lng:
          type: number
          nullable: true
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time

    LocationListResponse:
      type: object
      required: [items]
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/Location'
        next_cursor:
          type: string
          nullable: true

    # Activity schemas
    ActivityCreate:
      type: object
      required:
        - org_id
        - category_id
        - name
        - age_min
        - age_max
      properties:
        org_id:
          type: string
          format: uuid
          description: Organization UUID (required)
        category_id:
          type: string
          format: uuid
          description: Activity category UUID (required)
        name:
          type: string
          maxLength: 200
          description: Activity name (required, max 200 characters)
        description:
          type: string
          maxLength: 5000
          description: Activity description (optional, max 5,000 characters)
        name_translations:
          $ref: '#/components/schemas/TranslationMap'
          description: Non-English name translations (language map)
        description_translations:
          $ref: '#/components/schemas/TranslationMap'
          description: Non-English description translations (language map)
        age_min:
          type: integer
          minimum: 0
          maximum: 119
          description: Minimum age (required, 0-119, must be less than age_max)
        age_max:
          type: integer
          minimum: 1
          maximum: 120
          description: Maximum age (required, 1-120, must be greater than age_min)

    ActivityUpdate:
      type: object
      properties:
        category_id:
          type: string
          format: uuid
          description: Activity category UUID
        name:
          type: string
          maxLength: 200
          description: Activity name (max 200 characters)
        description:
          type: string
          maxLength: 5000
          description: Activity description (max 5,000 characters)
        name_translations:
          $ref: '#/components/schemas/TranslationMap'
          description: Non-English name translations (language map)
        description_translations:
          $ref: '#/components/schemas/TranslationMap'
          description: Non-English description translations (language map)
        age_min:
          type: integer
          minimum: 0
          maximum: 119
          description: Minimum age (must provide both age_min and age_max together)
        age_max:
          type: integer
          minimum: 1
          maximum: 120
          description: Maximum age (must provide both age_min and age_max together)

    Activity:
      type: object
      required:
        [
          id,
          org_id,
          category_id,
          name,
          name_translations,
          description_translations,
          age_min,
          age_max,
          created_at,
          updated_at,
        ]
      properties:
        id:
          type: string
          format: uuid
        org_id:
          type: string
          format: uuid
        category_id:
          type: string
          format: uuid
        name:
          type: string
        description:
          type: string
          nullable: true
        name_translations:
          $ref: '#/components/schemas/TranslationMap'
        description_translations:
          $ref: '#/components/schemas/TranslationMap'
        age_min:
          type: integer
        age_max:
          type: integer
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time

    ActivityListResponse:
      type: object
      required: [items]
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/Activity'
        next_cursor:
          type: string
          nullable: true

    # Pricing schemas
    PricingCreate:
      type: object
      required:
        - activity_id
        - location_id
        - pricing_type
        - amount
      properties:
        activity_id:
          type: string
          format: uuid
          description: Activity UUID (required)
        location_id:
          type: string
          format: uuid
          description: Location UUID (required)
        pricing_type:
          type: string
          enum: [per_class, per_sessions, per_hour, per_day, free]
          description: |
            Pricing type (required):
            - per_class: Price per individual class
            - per_sessions: Per term pricing (requires sessions_count)
            - per_hour: Hourly price
            - per_day: Daily price
            - free: Free pricing
        amount:
          type: number
          minimum: 0
          description: Price amount (required, must be >= 0)
        currency:
          type: string
          minLength: 3
          maxLength: 3
          pattern: '^[A-Za-z]{3}$'
          default: HKD
          description: ISO 4217 currency code (default HKD, case-insensitive)
        sessions_count:
          type: integer
          minimum: 1
          description: Classes within term (required for per_sessions, must be > 0)
        free_trial_class_offered:
          type: boolean
          description: Whether a free trial class is offered
          default: false

    PricingUpdate:
      type: object
      properties:
        pricing_type:
          type: string
          enum: [per_class, per_sessions, per_hour, per_day, free]
        amount:
          type: number
          minimum: 0
          description: Price amount (must be >= 0)
        currency:
          type: string
          minLength: 3
          maxLength: 3
          pattern: '^[A-Za-z]{3}$'
          description: ISO 4217 currency code (case-insensitive)
        sessions_count:
          type: integer
          minimum: 1
          description: Classes within term (must be > 0)
        free_trial_class_offered:
          type: boolean
          description: Whether a free trial class is offered

    Pricing:
      type: object
      required:
        [
          id,
          activity_id,
          location_id,
          pricing_type,
          amount,
          currency,
          free_trial_class_offered,
        ]
      properties:
        id:
          type: string
          format: uuid
        activity_id:
          type: string
          format: uuid
        location_id:
          type: string
          format: uuid
        pricing_type:
          type: string
        amount:
          type: number
        currency:
          type: string
        sessions_count:
          type: integer
          nullable: true
        free_trial_class_offered:
          type: boolean

    PricingListResponse:
      type: object
      required: [items]
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/Pricing'
        next_cursor:
          type: string
          nullable: true

    # Schedule schemas
    WeeklyScheduleEntry:
      type: object
      required:
        - day_of_week_utc
        - start_minutes_utc
        - end_minutes_utc
      properties:
        day_of_week_utc:
          type: integer
          minimum: 0
          maximum: 6
          description: |
            Day of week in UTC:
            0=Sunday, 1=Monday, 2=Tuesday, 3=Wednesday, 4=Thursday, 5=Friday, 6=Saturday
        start_minutes_utc:
          type: integer
          minimum: 0
          maximum: 1439
          description: |
            Start time in minutes from midnight UTC.
            Range: 0-1439 (0 = 00:00, 1439 = 23:59).
            Must not equal end_minutes_utc.
            If start_minutes_utc > end_minutes_utc, the schedule spans
            midnight UTC into the next day.
        end_minutes_utc:
          type: integer
          minimum: 0
          maximum: 1439
          description: |
            End time in minutes from midnight UTC.
            Range: 0-1439 (0 = 00:00, 1439 = 23:59).
            Must not equal start_minutes_utc.

    ScheduleCreate:
      type: object
      required:
        - activity_id
        - location_id
        - schedule_type
        - weekly_entries
      properties:
        activity_id:
          type: string
          format: uuid
          description: Activity UUID (required)
        location_id:
          type: string
          format: uuid
          description: Location UUID (required)
        schedule_type:
          type: string
          enum: [weekly]
          description: |
            Schedule type (required, weekly only).
        weekly_entries:
          type: array
          minItems: 1
          items:
            $ref: '#/components/schemas/WeeklyScheduleEntry'
          description: Weekly schedule entries (required).
        languages:
          type: array
          maxItems: 20
          items:
            type: string
            enum: [en, zh, ja, ko, fr, de, es, pt, it, ru, ar, hi, th, vi, id, ms, tl, nl, pl, tr, yue]
          description: |
            ISO 639-1 language codes plus "yue" (Cantonese, ISO 639-3)
            (optional, max 20).
            Supported: en (English), zh (Chinese), ja (Japanese), ko (Korean),
            fr (French), de (German), es (Spanish), pt (Portuguese), it (Italian),
            ru (Russian), ar (Arabic), hi (Hindi), th (Thai), vi (Vietnamese),
            id (Indonesian), ms (Malay), tl (Tagalog), nl (Dutch), pl (Polish),
            tr (Turkish), yue (Cantonese)

    ScheduleUpdate:
      type: object
      properties:
        schedule_type:
          type: string
          enum: [weekly]
        weekly_entries:
          type: array
          minItems: 1
          items:
            $ref: '#/components/schemas/WeeklyScheduleEntry'
        languages:
          type: array
          maxItems: 20
          items:
            type: string
            enum: [en, zh, ja, ko, fr, de, es, pt, it, ru, ar, hi, th, vi, id, ms, tl, nl, pl, tr, yue]

    Schedule:
      type: object
      required: [id, activity_id, location_id, schedule_type, weekly_entries, languages]
      properties:
        id:
          type: string
          format: uuid
        activity_id:
          type: string
          format: uuid
        location_id:
          type: string
          format: uuid
        schedule_type:
          type: string
        weekly_entries:
          type: array
          items:
            $ref: '#/components/schemas/WeeklyScheduleEntry'
        languages:
          type: array
          items:
            type: string

    ScheduleListResponse:
      type: object
      required: [items]
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/Schedule'
        next_cursor:
          type: string
          nullable: true

    # Media upload schemas
    MediaUploadRequest:
      type: object
      required:
        - file_name
        - content_type
      properties:
        file_name:
          type: string
          description: Original file name
        content_type:
          type: string
          pattern: '^image/'
          description: MIME type (must start with 'image/')

    MediaUploadResponse:
      type: object
      required: [upload_url, media_url, object_key, expires_in]
      properties:
        upload_url:
          type: string
          description: Presigned S3 upload URL (expires in 15 minutes)
        media_url:
          type: string
          description: Final public URL of the media file
        object_key:
          type: string
          description: S3 object key
        expires_in:
          type: integer
          description: URL expiration time in seconds

    MediaDeleteRequest:
      type: object
      properties:
        object_key:
          type: string
          description: S3 object key to delete
        media_url:
          type: string
          description: Media URL to delete (alternative to object_key)

    # Feedback label schemas
    FeedbackLabel:
      type: object
      required: [id, name, name_translations, display_order]
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string
        name_translations:
          type: object
          additionalProperties:
            type: string
        display_order:
          type: integer

    FeedbackLabelCreate:
      type: object
      required: [name]
      properties:
        name:
          type: string
          maxLength: 200
        name_translations:
          type: object
          additionalProperties:
            type: string
        display_order:
          type: integer
          minimum: 0

    FeedbackLabelUpdate:
      type: object
      properties:
        name:
          type: string
          maxLength: 200
        name_translations:
          type: object
          additionalProperties:
            type: string
        display_order:
          type: integer
          minimum: 0

    FeedbackLabelListResponse:
      type: object
      required: [items]
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/FeedbackLabel'
        next_cursor:
          type: string
          nullable: true

    # Organization feedback schemas
    OrganizationFeedback:
      type: object
      required: [id, organization_id, stars, label_ids, created_at, updated_at]
      properties:
        id:
          type: string
          format: uuid
        organization_id:
          type: string
          format: uuid
        organization_name:
          type: string
          nullable: true
        submitter_id:
          type: string
          nullable: true
        submitter_email:
          type: string
          nullable: true
        stars:
          type: integer
          minimum: 0
          maximum: 5
        label_ids:
          type: array
          items:
            type: string
            format: uuid
        description:
          type: string
          nullable: true
        source_ticket_id:
          type: string
          nullable: true
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time

    OrganizationFeedbackCreate:
      type: object
      required: [organization_id, stars]
      properties:
        organization_id:
          type: string
          format: uuid
        stars:
          type: integer
          minimum: 0
          maximum: 5
        label_ids:
          type: array
          items:
            type: string
            format: uuid
        description:
          type: string
          maxLength: 5000
        submitter_id:
          type: string
          maxLength: 200
        submitter_email:
          type: string
          maxLength: 320
        source_ticket_id:
          type: string
          maxLength: 200

    OrganizationFeedbackUpdate:
      type: object
      properties:
        organization_id:
          type: string
          format: uuid
        stars:
          type: integer
          minimum: 0
          maximum: 5
        label_ids:
          type: array
          items:
            type: string
            format: uuid
        description:
          type: string
          maxLength: 5000
        submitter_id:
          type: string
          maxLength: 200
        submitter_email:
          type: string
          maxLength: 320
        source_ticket_id:
          type: string
          maxLength: 200

    OrganizationFeedbackListResponse:
      type: object
      required: [items]
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/OrganizationFeedback'
        next_cursor:
          type: string
          nullable: true

    UserFeedbackCreate:
      type: object
      required: [organization_id, stars]
      properties:
        organization_id:
          type: string
          format: uuid
        stars:
          type: integer
          minimum: 0
          maximum: 5
        label_ids:
          type: array
          items:
            type: string
            format: uuid
        description:
          type: string
          maxLength: 5000

    UserFeedbackResponse:
      type: object
      required: [has_pending_feedback, feedbacks]
      properties:
        has_pending_feedback:
          type: boolean
        feedbacks:
          type: array
          items:
            $ref: '#/components/schemas/Ticket'

    OrganizationLookup:
      type: object
      required: [id, name]
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string

    OrganizationLookupListResponse:
      type: object
      required: [items]
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/OrganizationLookup'

    # Cognito User schemas
    CognitoUser:
      type: object
      required: [sub, email, email_verified, username, status, enabled, groups, created_at, updated_at, attributes]
      properties:
        sub:
          type: string
          description: Cognito user sub (UUID)
        email:
          type: string
        email_verified:
          type: boolean
        name:
          type: string
          nullable: true
        given_name:
          type: string
          nullable: true
        family_name:
          type: string
          nullable: true
        username:
          type: string
        status:
          type: string
          description: Cognito user status (e.g., CONFIRMED, FORCE_CHANGE_PASSWORD)
        enabled:
          type: boolean
        groups:
          type: array
          items:
            type: string
          description: >-
            Memberships in the managed Cognito groups (admin, manager),
            resolved per group rather than per user.
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
        last_auth_time:
          type: string
          format: date-time
          nullable: true
        attributes:
          type: object
          additionalProperties:
            type: string

    CognitoUserListResponse:
      type: object
      required: [items]
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/CognitoUser'
        pagination_token:
          type: string
          nullable: true
          description: Token for fetching the next page

    # Access Request input schema (used by user-facing POST endpoint)
    AccessRequestCreate:
      type: object
      required:
        - organization_name
      properties:
        organization_name:
          type: string
          maxLength: 200
          description: Name of the organization to request access to
        request_message:
          type: string
          maxLength: 5000
          description: Optional message to the admin

    # Ticket schemas
    Ticket:
      type: object
      required: [id, ticket_id, ticket_type, organization_name, media_urls, status, submitter_id, submitter_email, created_at]
      properties:
        id:
          type: string
          format: uuid
        ticket_id:
          type: string
          description: Unique progressive ticket ID (prefix + 5 digits)
        ticket_type:
          type: string
          enum: [access_request, organization_suggestion, organization_feedback]
        organization_name:
          type: string
        message:
          type: string
          nullable: true
          description: Free-text message from the submitter
        status:
          type: string
          enum: [pending, approved, rejected]
        submitter_id:
          type: string
          description: Cognito user sub of the submitter
        submitter_email:
          type: string
        created_at:
          type: string
          format: date-time
        reviewed_at:
          type: string
          format: date-time
          nullable: true
        reviewed_by:
          type: string
          nullable: true
        admin_notes:
          type: string
          nullable: true
        description:
          type: string
          nullable: true
          description: Extended description (optional, depends on ticket type)
        suggested_district:
          type: string
          nullable: true
        suggested_address:
          type: string
          nullable: true
        suggested_lat:
          type: number
          nullable: true
        suggested_lng:
          type: number
          nullable: true
        media_urls:
          type: array
          items:
            type: string
        organization_id:
          type: string
          format: uuid
          nullable: true
        feedback_stars:
          type: integer
          minimum: 0
          maximum: 5
          nullable: true
        feedback_label_ids:
          type: array
          items:
            type: string
            format: uuid
        feedback_text:
          type: string
          nullable: true
        created_organization_id:
          type: string
          format: uuid
          nullable: true

    TicketReview:
      type: object
      required:
        - action
      properties:
        action:
          type: string
          enum: [approve, reject]
        admin_notes:
          type: string
          description: Notes from the admin about the decision
        organization_id:
          type: string
          format: uuid
          description: Assign an existing organization on approval
        create_organization:
          type: boolean
          description: Create a new organization on approval

    TicketReviewResponse:
      type: object
      required: [message, ticket]
      properties:
        message:
          type: string
        ticket:
          $ref: '#/components/schemas/Ticket'
        organization:
          $ref: '#/components/schemas/Organization'
          nullable: true
          description: The organization created or assigned (only on approval)

    TicketListResponse:
      type: object
      required: [items, pending_count]
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/Ticket'
        next_cursor:
          type: string
          nullable: true
        pending_count:
          type: integer
          description: Number of pending tickets matching the current filters

    # Audit Log schemas
    AuditLogEntry:
      type: object
      required: [id, timestamp, table_name, record_id, action, source]
      properties:
        id:
          type: string
          format: uuid
        timestamp:
          type: string
          format: date-time
        table_name:
          type: string
        record_id:
          type: string
        action:
          type: string
          enum: [INSERT, UPDATE, DELETE]
        user_id:
          type: string
          nullable: true
        request_id:
          type: string
          nullable: true
        changed_fields:
          type: array
          items:
            type: string
          nullable: true
        old_values:
          type: object
          additionalProperties: true
          nullable: true
          description: Previous field values (sensitive fields redacted)
        new_values:
          type: object
          additionalProperties: true
          nullable: true
          description: New field values (sensitive fields redacted)
        source:
          type: string
          description: Source of the audit entry (trigger or application)
        ip_address:
          type: string
          nullable: true
          description: Client IP address if available
        user_agent:
          type: string
          nullable: true
          description: Client user agent if available

    AuditLogListResponse:
      type: object
      required: [items]
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/AuditLogEntry'
        next_cursor:
          type: string
          nullable: true

    # Partner API key schemas
    ApiKey:
      type: object
      required: [id, name, key_prefix, scope, status, created_at]
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string
          description: Human-readable label for the key
        key_prefix:
          type: string
          description: First characters of the plaintext key, for display
        scope:
          type: string
          enum: [read, crud]
          description: read = GET only; crud = full CRUD on partner routes
        org_id:
          type: string
          format: uuid
          nullable: true
          description: Organization the key is scoped to; null = full access
        status:
          type: string
          enum: [active, revoked, expired]
        created_by:
          type: string
          nullable: true
          description: Cognito sub of the admin who created the key
        created_at:
          type: string
          format: date-time
        expires_at:
          type: string
          format: date-time
          nullable: true
        revoked_at:
          type: string
          format: date-time
          nullable: true
        last_used_at:
          type: string
          format: date-time
          nullable: true

    ApiKeyCreate:
      type: object
      required: [name, scope]
      properties:
        name:
          type: string
          maxLength: 200
          description: Human-readable label for the key
        scope:
          type: string
          enum: [read, crud]
        org_id:
          type: string
          format: uuid
          nullable: true
          description: Scope the key to one organization; omit for full access
        expires_at:
          type: string
          format: date-time
          nullable: true
          description: Optional expiry (ISO 8601, must be in the future)

    ApiKeyCreated:
      allOf:
        - $ref: '#/components/schemas/ApiKey'
        - type: object
          required: [api_key]
          properties:
            api_key:
              type: string
              description: |
                The plaintext API key. Returned only once at creation and
                never stored.

    ApiKeyListResponse:
      type: object
      required: [items]
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/ApiKey'
        next_cursor:
          type: string
          nullable: true

    # Activity Category schemas
    ActivityCategory:
      type: object
      required: [id, name, name_translations, display_order]
      properties:
        id:
          type: string
          format: uuid
        parent_id:
          type: string
          format: uuid
          nullable: true
          description: NULL for root category nodes
        name:
          type: string
          description: Category name
        name_translations:
          $ref: '#/components/schemas/TranslationMap'
        display_order:
          type: integer
        children:
          type: array
          items:
            $ref: '#/components/schemas/ActivityCategory'
          description: Nested child categories (tree responses)

    ActivityCategoryCreate:
      type: object
      required: [name]
      properties:
        name:
          type: string
          maxLength: 200
          description: Category name (required)
        name_translations:
          $ref: '#/components/schemas/TranslationMap'
          description: Non-English name translations (language map)
        parent_id:
          type: string
          format: uuid
          nullable: true
          description: Optional parent category UUID
        display_order:
          type: integer
          description: Sort order within the parent

    ActivityCategoryUpdate:
      type: object
      properties:
        name:
          type: string
          maxLength: 200
          description: Category name
        name_translations:
          $ref: '#/components/schemas/TranslationMap'
          description: Non-English name translations (language map)
        parent_id:
          type: string
          format: uuid
          nullable: true
          description: Parent category UUID or null for root
        display_order:
          type: integer
          description: Sort order within the parent

    ActivityCategoryListResponse:
      type: object
      required: [items]
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/ActivityCategory'
        next_cursor:
          type: string
          nullable: true

    ActivityCategoryTreeResponse:
      type: object
      required: [items]
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/ActivityCategory'

    # Geographic Area schemas
    GeographicArea:
      type: object
      required: [id, name, name_translations, level, active, display_order]
      properties:
        id:
          type: string
          format: uuid
        parent_id:
          type: string
          format: uuid
          nullable: true
          description: NULL for root (country) nodes
        name:
          type: string
          description: Area name (e.g., 'Hong Kong', 'Wan Chai')
        name_translations:
          $ref: '#/components/schemas/TranslationMap'
        level:
          type: string
          enum: [country, region, city, district]
        code:
          type: string
          nullable: true
          description: ISO 3166-1 alpha-2 for countries (HK, SG, AE)
        active:
          type: boolean
          description: Whether this area (and its children) is available
        display_order:
          type: integer
        children:
          type: array
          items:
            $ref: '#/components/schemas/GeographicArea'
          description: Nested child areas (populated in tree responses)

    AreaTreeResponse:
      type: object
      required: [items]
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/GeographicArea'

    AddressSearchResponse:
      type: object
      required: [items]
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/NominatimResult'

    NominatimResult:
      type: object
      required: [place_id, display_name, lat, lon, address]
      properties:
        place_id:
          type: integer
        display_name:
          type: string
        lat:
          type: string
        lon:
          type: string
        address:
          $ref: '#/components/schemas/NominatimAddress'
        type:
          type: string

    NominatimAddress:
      type: object
      additionalProperties:
        type: string

    # Organization Suggestion input schema (used by user-facing POST endpoint)
    OrganizationSuggestionCreate:
      type: object
      required:
        - organization_name
      properties:
        organization_name:
          type: string
          maxLength: 200
          description: Suggested organization name (required)
        description:
          type: string
          maxLength: 5000
          description: Organization description
        suggested_district:
          type: string
          maxLength: 100
          description: Suggested district
        suggested_address:
          type: string
          maxLength: 500
          description: Suggested address
        suggested_lat:
          type: number
          minimum: -90
          maximum: 90
          description: Latitude of the suggested location (-90 to 90)
        suggested_lng:
          type: number
          minimum: -180
          maximum: 180
          description: Longitude of the suggested location (-180 to 180)
        media_urls:
          type: array
          maxItems: 20
          items:
            type: string
            format: uri
            maxLength: 2048
          description: Optional media URLs for the suggestion
        additional_notes:
          type: string
          maxLength: 5000
          description: Additional notes for the admin

    # Health Check schemas
    HealthStatus:
      type: object
      required: [healthy]
      properties:
        healthy:
          type: boolean
        version:
          type: string
        environment:
          type: string
        checks:
          type: array
          items:
            $ref: '#/components/schemas/HealthCheckResult'

    HealthCheckResult:
      type: object
      required: [name, healthy]
      properties:
        name:
          type: string
        healthy:
          type: boolean
        latency_ms:
          type: number
          nullable: true
        error:
          type: string
          nullable: true
        details:
          type: object
          nullable: true
